"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.quietCompile = void 0;
const task_names_1 = require("hardhat/builtin-tasks/task-names");
/*
 * Note: Even though hardhat's compile task has a quiet option,
 * it still prints some output. This is a hack to completely silence
 * output during compile task run.
 */
async function quietCompile(hre, quiet) {
    let logCache;
    if (quiet) {
        logCache = console.log;
        console.log = () => { };
    }
    try {
        await hre.run(task_names_1.TASK_COMPILE, { force: true, quiet: true });
    }
    finally {
        if (logCache)
            console.log = logCache;
    }
}
exports.quietCompile = quietCompile;
//# sourceMappingURL=quiet-compile.js.map