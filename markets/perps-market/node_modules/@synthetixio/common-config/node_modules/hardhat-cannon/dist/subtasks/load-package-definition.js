"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_path_1 = __importDefault(require("node:path"));
const cli_1 = require("@usecannon/cli");
const config_1 = require("hardhat/config");
const task_names_1 = require("../task-names");
(0, config_1.subtask)(task_names_1.SUBTASK_LOAD_PACKAGE_DEFINITION).setAction(async ({ packageWithSettingsParams }, hre) => {
    if (!packageWithSettingsParams || !packageWithSettingsParams.length) {
        return getDefaultPackageDefinition(hre);
    }
    let packageSpec;
    try {
        return packageWithSettingsParams.reduce((result, val) => {
            return (0, cli_1.parsePackageArguments)(val, result);
        }, {});
    }
    catch (err) {
        packageSpec = await getDefaultPackageDefinition(hre);
        return (packageSpec = packageWithSettingsParams.reduce((result, val) => {
            return (0, cli_1.parsePackageArguments)(val, result);
        }, packageSpec));
    }
});
async function getDefaultPackageDefinition(hre) {
    const { name, version } = await (0, cli_1.loadCannonfile)(node_path_1.default.join(hre.config.paths.root, 'cannonfile.toml'));
    return {
        name,
        version,
        settings: {},
    };
}
//# sourceMappingURL=load-package-definition.js.map