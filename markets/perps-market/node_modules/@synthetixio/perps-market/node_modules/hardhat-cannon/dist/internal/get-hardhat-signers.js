"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getHardhatSigners = void 0;
const accounts_1 = require("viem/accounts");
function getHardhatSigners(hre /*, provider: viem.WalletClient*/) {
    const accounts = hre.network.config.accounts;
    let signers = [];
    if (Array.isArray(accounts)) {
        signers = accounts.map((k) => (0, accounts_1.privateKeyToAccount)(k));
    }
    else if (accounts === 'remote') {
        // TODO
        //signers = hre.
    }
    else {
        signers = Array(accounts.count)
            .fill(0)
            .map((_, i) => (0, accounts_1.mnemonicToAccount)(accounts.mnemonic, { path: (accounts.path + `/${i + accounts.initialIndex}`) }));
    }
    return signers;
}
exports.getHardhatSigners = getHardhatSigners;
//# sourceMappingURL=get-hardhat-signers.js.map