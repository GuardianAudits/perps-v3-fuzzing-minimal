import { Command, CommandType, ListCommandOption, ProjectConfig } from '@lerna-lite/core';
import { FilterOptions } from '@lerna-lite/filter-packages';
export declare function factory(argv: ListCommandOption): ListCommand;
export declare class ListCommand extends Command<ListCommandOption & FilterOptions> {
    name: CommandType;
    result?: {
        count: number;
        text: string;
    };
    constructor(argv: ListCommandOption | ProjectConfig);
    get requiresGit(): boolean;
    initialize(): Promise<void>;
    execute(): void;
}
