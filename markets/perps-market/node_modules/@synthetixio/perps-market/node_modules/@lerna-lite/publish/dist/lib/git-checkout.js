import log from 'npmlog';
import { exec } from '@lerna-lite/core';
export function gitCheckout(stagedFiles, gitOpts, execOpts, dryRun = false) {
    const files = (gitOpts.granularPathspec ? stagedFiles : '.');
    log.silly('gitCheckout', files);
    return exec('git', ['checkout', '--'].concat(files), execOpts, dryRun);
}
//# sourceMappingURL=git-checkout.js.map