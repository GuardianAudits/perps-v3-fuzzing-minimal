/// <reference types="node" />
import fs from 'graceful-fs';
import { Readable } from 'node:stream';
export declare function tempWrite(fileContent: Readable | fs.PathOrFileDescriptor, filePath?: string): Promise<string>;
export declare namespace tempWrite {
    var sync: (fileContent: string | (DataView & Readable), filePath?: string | undefined) => string;
}
